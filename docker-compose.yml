services:
  webapp:
    build: .
    container_name: notes-web-app
    environment:
      - PORT=3000
      - WEB_APP_NOTES_MONGODB_HOST=mongo:27017
      - WEB_APP_NOTES_MONGODB_DATABASE=notesdb
    depends_on:
      - mongo
    networks:
      - backend

  mongo:
    image: mongo
    container_name: notes-mongo
    volumes:
      - mongodata:/data/db
    networks:
      - backend

  mongo-express:
    image: mongo-express
    container_name: notes-mongo-express
    environment:
      - ME_CONFIG_MONGODB_SERVER=mongo
      - ME_CONFIG_BASICAUTH=false
    ports:
      - "8081:8081"
    depends_on:
      - mongo
    networks:
      - frontend
      - backend

  nginx:
    image: nginx
    container_name: notes-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/certs:/etc/nginx/certs:ro
    depends_on:
      - webapp
    networks:
      - frontend
      - backend

volumes:
  mongodata:

networks:
  backend:
    internal: true
  frontend:
